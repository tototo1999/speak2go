# Codex Speak2GO Preview

Public preview path: `/Codex-speak2go/`

This is a static front-end preview copied from the current working tree. It uses the existing Speak2GO Supabase shared backend and keeps direct production Modal worker URLs disabled in the preview copy.

Do not use this directory as the canonical source. Apply feature work to the root files first, then refresh this preview copy when needed.
